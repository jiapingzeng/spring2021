<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>List</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    </head>
    <body>
        <div class="container">
            <table class="table table-striped my-3">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Created</th>
                        <th>Modified</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    <% for (post of posts) { %>
                        <tr>
                            <td><%= post.title %></td>
                            <td><%= new Date(post.created).toLocaleString() %></td>
                            <td><%= new Date(post.modified).toLocaleString() %></td>
                            <td><a href="/blog/<%= username %>/<%= post.postid %>">open</a></td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
            <% if (hasNext) { %>
                <a id="/blog/<%= username %>?start=<%= start+5 %>" href="/blog/<%= username %>?start=<%= start+5 %>">next</a>
            <% } %>
        </div>
    </body>
</html>
